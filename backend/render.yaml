services:
  - type: web
    name: kk39-ic-backend
    env: python
    buildCommand: "pip install -r requirements.txt && npm install -g docx"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
